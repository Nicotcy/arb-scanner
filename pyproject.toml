[project]
name = "arb-scanner"
version = "0.1.0"
description = "Read-only scanner for Kalshi and Polymarket"
requires-python = ">=3.10"
dependencies = ["requests>=2.32.0"]

[project.scripts]
arb-scanner = "arb_scanner.scanner:main"
